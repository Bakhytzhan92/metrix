{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "cd backend && python manage.py collectstatic --noinput && python -m gunicorn gectaro_saas.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
